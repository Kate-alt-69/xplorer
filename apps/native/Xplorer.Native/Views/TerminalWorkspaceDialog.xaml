<ContentDialog
    x:Class="Xplorer.Native.Views.TerminalWorkspaceDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ContentDialog.Resources>
        <x:Double x:Key="ContentDialogMinWidth">640</x:Double>
        <x:Double x:Key="ContentDialogMaxWidth">1440</x:Double>
    </ContentDialog.Resources>

    <Grid
        x:Name="DialogRoot"
        Width="980"
        Height="620"
        MinWidth="640"
        MaxWidth="1440"
        MinHeight="400"
        MaxHeight="900">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Margin="2,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Spacing="2">
                <StackPanel Orientation="Horizontal" Spacing="9">
                    <FontIcon Glyph="&#xE756;" FontSize="17" VerticalAlignment="Center" />
                    <TextBlock Text="Terminal" FontSize="22" FontWeight="SemiBold" />
                </StackPanel>
                <TextBlock
                    Text="In-app ConPTY terminal • tabs survive while this window is hidden"
                    Opacity="0.62"
                    FontSize="11" />
            </StackPanel>
            <Button
                Grid.Column="1"
                Width="36"
                Height="36"
                Padding="0"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                ToolTipService.ToolTip="Hide terminal (Tab + T)"
                Click="TerminalClose_Click">
                <FontIcon Glyph="&#xE711;" FontSize="13" />
            </Button>
        </Grid>

        <Grid Grid.Row="1">
            <Border
                CornerRadius="10"
                BorderThickness="1"
                BorderBrush="{ThemeResource XplorerBorderBrush}"
                Background="#0C0C0C"
                Padding="1">
                <TabView
                    x:Name="TerminalTabs"
                    Background="#0C0C0C"
                    Foreground="#F2F2F2"
                    IsAddTabButtonVisible="True"
                    CanReorderTabs="True"
                    TabWidthMode="SizeToContent"
                    AddTabButtonClick="TerminalTabs_AddTabButtonClick"
                    TabCloseRequested="TerminalTabs_TabCloseRequested"
                    SelectionChanged="TerminalTabs_SelectionChanged" />
            </Border>

            <Border
                x:Name="ResizeGrip"
                Width="22"
                Height="22"
                Margin="0,0,2,2"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Background="Transparent"
                ToolTipService.ToolTip="Drag to resize • double-click to reset"
                PointerPressed="ResizeGrip_PointerPressed"
                PointerMoved="ResizeGrip_PointerMoved"
                PointerReleased="ResizeGrip_PointerReleased"
                PointerCanceled="ResizeGrip_PointerReleased"
                DoubleTapped="ResizeGrip_DoubleTapped">
                <FontIcon
                    Glyph="&#xE8B9;"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontSize="12"
                    Opacity="0.52" />
            </Border>
        </Grid>
    </Grid>
</ContentDialog>
